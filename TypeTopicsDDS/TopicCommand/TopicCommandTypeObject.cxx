// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file TopicCommandTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool gen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace { char dummy; }
#endif

#include "TopicCommand.h"
#include "TopicCommandTypeObject.h"
#include <utility>
#include <sstream>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>
#include <fastcdr/FastBuffer.h>
#include <fastcdr/Cdr.h>

using namespace eprosima::fastrtps::rtps;

void registerTopicCommandTypes()
{
    TypeObjectFactory *factory = TypeObjectFactory::get_instance();
    factory->add_type_object("TopicCommand", GetTopicCommandIdentifier(true), GetTopicCommandObject(true));
    factory->add_type_object("TopicCommand", GetTopicCommandIdentifier(false), GetTopicCommandObject(false));

}

const TypeIdentifier* GetTopicCommandIdentifier(bool complete)
{
    const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("TopicCommand", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    GetTopicCommandObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("TopicCommand", complete);
}

const TypeObject* GetTopicCommandObject(bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("TopicCommand", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteTopicCommandObject();
    }
    //else
    return GetMinimalTopicCommandObject();
}

const TypeObject* GetMinimalTopicCommandObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("TopicCommand", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_time_messege;
    mst_time_messege.common().member_id(memberId++);
    mst_time_messege.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_time_messege.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_time_messege.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_time_messege.common().member_flags().IS_OPTIONAL(false);
    mst_time_messege.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_time_messege.common().member_flags().IS_KEY(false);
    mst_time_messege.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "int64_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        mst_time_messege.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    MD5 time_messege_hash("time_messege");
    for(int i = 0; i < 4; ++i)
    {
        mst_time_messege.detail().name_hash()[i] = time_messege_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_time_messege);

    MinimalStructMember mst_id_source;
    mst_id_source.common().member_id(memberId++);
    mst_id_source.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_id_source.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_id_source.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_id_source.common().member_flags().IS_OPTIONAL(false);
    mst_id_source.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_id_source.common().member_flags().IS_KEY(false);
    mst_id_source.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "uint32_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        mst_id_source.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    MD5 id_source_hash("id_source");
    for(int i = 0; i < 4; ++i)
    {
        mst_id_source.detail().name_hash()[i] = id_source_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_id_source);

    MinimalStructMember mst_id_target;
    mst_id_target.common().member_id(memberId++);
    mst_id_target.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_id_target.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_id_target.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_id_target.common().member_flags().IS_OPTIONAL(false);
    mst_id_target.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_id_target.common().member_flags().IS_KEY(false);
    mst_id_target.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "uint32_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        mst_id_target.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    MD5 id_target_hash("id_target");
    for(int i = 0; i < 4; ++i)
    {
        mst_id_target.detail().name_hash()[i] = id_target_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_id_target);

    MinimalStructMember mst_code_command;
    mst_code_command.common().member_id(memberId++);
    mst_code_command.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_code_command.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_code_command.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_code_command.common().member_flags().IS_OPTIONAL(false);
    mst_code_command.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_code_command.common().member_flags().IS_KEY(false);
    mst_code_command.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "uint32_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        mst_code_command.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    MD5 code_command_hash("code_command");
    for(int i = 0; i < 4; ++i)
    {
        mst_code_command.detail().name_hash()[i] = code_command_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_code_command);

    MinimalStructMember mst_parameter;
    mst_parameter.common().member_id(memberId++);
    mst_parameter.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_parameter.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_parameter.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_parameter.common().member_flags().IS_OPTIONAL(false);
    mst_parameter.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_parameter.common().member_flags().IS_KEY(false);
    mst_parameter.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_parameter.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {256}, false));


    MD5 parameter_hash("parameter");
    for(int i = 0; i < 4; ++i)
    {
        mst_parameter.detail().name_hash()[i] = parameter_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_parameter);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];

    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    SerializedPayload_t payload(static_cast<uint32_t>(
        MinimalStructType::getCdrSerializedSize(type_object->minimal().struct_type()) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
    payload.encapsulation = CDR_LE;

    type_object->serialize(ser);
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("TopicCommand", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("TopicCommand", false);
}

const TypeObject* GetCompleteTopicCommandObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("TopicCommand", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_time_messege;
    cst_time_messege.common().member_id(memberId++);
    cst_time_messege.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_time_messege.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_time_messege.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_time_messege.common().member_flags().IS_OPTIONAL(false);
    cst_time_messege.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_time_messege.common().member_flags().IS_KEY(false);
    cst_time_messege.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "int64_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        cst_time_messege.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    cst_time_messege.detail().name("time_messege");

    type_object->complete().struct_type().member_seq().emplace_back(cst_time_messege);

    CompleteStructMember cst_id_source;
    cst_id_source.common().member_id(memberId++);
    cst_id_source.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_id_source.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_id_source.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_id_source.common().member_flags().IS_OPTIONAL(false);
    cst_id_source.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_id_source.common().member_flags().IS_KEY(false);
    cst_id_source.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "uint32_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        cst_id_source.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    cst_id_source.detail().name("id_source");

    type_object->complete().struct_type().member_seq().emplace_back(cst_id_source);

    CompleteStructMember cst_id_target;
    cst_id_target.common().member_id(memberId++);
    cst_id_target.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_id_target.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_id_target.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_id_target.common().member_flags().IS_OPTIONAL(false);
    cst_id_target.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_id_target.common().member_flags().IS_KEY(false);
    cst_id_target.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "uint32_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        cst_id_target.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    cst_id_target.detail().name("id_target");

    type_object->complete().struct_type().member_seq().emplace_back(cst_id_target);

    CompleteStructMember cst_code_command;
    cst_code_command.common().member_id(memberId++);
    cst_code_command.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_code_command.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_code_command.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_code_command.common().member_flags().IS_OPTIONAL(false);
    cst_code_command.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_code_command.common().member_flags().IS_KEY(false);
    cst_code_command.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    {
        std::string cppType = "uint32_t";
        if (cppType == "long double")
        {
            cppType = "longdouble";
        }
        cst_code_command.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier(cppType, false));
    }

    cst_code_command.detail().name("code_command");

    type_object->complete().struct_type().member_seq().emplace_back(cst_code_command);

    CompleteStructMember cst_parameter;
    cst_parameter.common().member_id(memberId++);
    cst_parameter.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_parameter.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_parameter.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_parameter.common().member_flags().IS_OPTIONAL(false);
    cst_parameter.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_parameter.common().member_flags().IS_KEY(false);
    cst_parameter.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_parameter.common().member_type_id(*TypeObjectFactory::get_instance()->get_array_identifier("char", {256}, true));


    cst_parameter.detail().name("parameter");

    type_object->complete().struct_type().member_seq().emplace_back(cst_parameter);


    // Header
    type_object->complete().struct_type().header().detail().type_name("TopicCommand");
    // TODO inheritance


    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    SerializedPayload_t payload(static_cast<uint32_t>(
        CompleteStructType::getCdrSerializedSize(type_object->complete().struct_type()) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::Cdr::DDS_CDR); // Object that serializes the data.
    payload.encapsulation = CDR_LE;

    type_object->serialize(ser);
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("TopicCommand", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("TopicCommand", true);
}
